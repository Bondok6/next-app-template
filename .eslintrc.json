{
  "extends": ["next", "next/core-web-vitals", "eslint:recommended"],
  "globals": {
    // You don't have to import react in your code.
    "React": "readonly"
  },
  "rules": {
    // prefix unused variables with _
    "no-unused-vars": [1, { "args": "after-used", "argsIgnorePattern": "^_" }]
  }
}
